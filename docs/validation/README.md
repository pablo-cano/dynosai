# Release Validation Evidence

This directory contains selected machine-readable evidence supporting public release claims. It is intentionally small; raw development logs and private provider transcripts should not be committed by default.

## 0.13.0

- `final-matrix-0.13.0.json` — final Codex/Cursor × Greenfield/Brownfield beta baseline certification.
- `predictive-router-0.13.0.json` — offline historical replay used to decide that predictive routing should remain shadow-only in the beta runtime baseline.

The final matrix passed 4/4 with no infrastructure retries.
